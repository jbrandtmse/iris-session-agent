# Story 14.2: Schema-Discovery Tools (FR61)

Status: done

**Source:** `sprint-change-proposal-2026-06-10.md` Â§4.1 FR61, Â§3.2 principle 3 ("Discovery as typed tools â€” catalog queries are deterministic; wrap them as cheap tools rather than having the LLM author them"). Knowledge grounding: `docs/iris-query-guide/04-schema-discovery.md` (the workflow these tools operationalize) + `02-interop-message-model.md` (classâ†’table projection).

## Story

As an **operator asking about message types the fixed tools don't anticipate**, I want typed schema-discovery tools (`list_active_body_types`, `describe_message_class`, `discover_tables`), so that the agents discover live schema deterministically instead of hallucinating table/column names into generated SQL.

## Acceptance Criteria

**AC-1 â€” `list_active_body_types`.** New class `SessionAgent.Tool.Inspection.ListActiveBodyTypes` extends `Tool.Base`. Args: `hours` (optional integer, default 168, clamp 1..2160). Runs a windowed census: `SELECT MessageBodyClassName, COUNT(*) ... FROM Ens.MessageHeader WHERE TimeCreated >= <window> GROUP BY MessageBodyClassName ORDER BY count DESC` (parameterized window; `%EXACT` per case rules). Result rows: body class name, message count; empty-name rows surfaced as `(no body)`. `render_strategy:"ok"` + row count; empty census is NOT an error (`body_types:[]`).

> **Then** live dispatch with default window returns â‰¥1 row on the sample production (OrderIngest scenarios populate `SessionAgent.Sample.Msg.*` bodies); verbatim envelope captured.

**AC-2 â€” `describe_message_class`.** New class `SessionAgent.Tool.Inspection.DescribeMessageClass` extends `Tool.Base`. Arg: `class_name` (required; server-side `$Match(tClassName, "^[A-Za-z%][A-Za-z0-9%._]*$")` validation; parameterized SQL). Returns, for an existing compiled persistent class: (a) authoritative `schema.table` mapping via `%Dictionary.CompiledClass` (`SqlSchemaName`/`SqlTableName`); (b) columns + datatypes via `INFORMATION_SCHEMA.COLUMNS`; (c) indexes (`INFORMATION_SCHEMA.INDEXES` or `%Dictionary.CompiledIndex`); (d) subclass enumeration (`%Dictionary.CompiledClass` WHERE Super-chain match â€” `UPPER()` both sides on any `%Dictionary` string joins per the epics.md callout); (e) collection child tables (list/array properties projecting child tables); (f) when the class has an `AdditionalInfo` array property (`Ens.MessageBody` descendants), a key census from the projected child table (TOP-bounded). Non-existent / non-persistent class â†’ distinct `render_strategy:"class_not_found"` (not a fault). Result char budget â‰ˆ 16 KB with `truncated` marker.

> **Then** live dispatch for a `SessionAgent.Sample.Msg.*` class returns table mapping + columns + indexes; dispatch for `Ens.MessageHeader` enumerates â‰¥1 subclass-or-self correctly; bogus name returns `class_not_found`; malformed name returns validation envelope.

**AC-3 â€” `discover_tables`.** New class `SessionAgent.Tool.Inspection.DiscoverTables` extends `Tool.Base`. Args: `name_fragment` (optional string, parameterized LIKE both-sides-UPPER), `include_system` (optional boolean, default 0 â€” excludes `%*`, `INFORMATION_SCHEMA`, `Ens_*`-internal? NO: `Ens*` schemas are operator-relevant and stay INCLUDED; excluded set is `%` and `INFORMATION_SCHEMA` schemas only), `max_results` (default 50, clamp 1..200). Queries `INFORMATION_SCHEMA.TABLES`. Result rows: schema, table, row-type (BASE TABLE/VIEW).

> **Then** live dispatch with `name_fragment="MessageHeader"` finds `Ens.MessageHeader`; default call excludes `%`-schemas; verbatim envelope captured.

**AC-4 â€” Suite updates.** ISV: **EXPECTEDTOOLCOUNT 29â†’32 AND all 3 tool names added to `tExpected` $ListBuild AND `GetRepresentativeArgs` entries** (representative args must dispatch successfully against the fixture/sample data). `ToolCallRoundtripIntegrationTest.TestMatrixCardinalityMatchesCatalog`: 116â†’128 (4 Ã— 32) + comment arithmetic.

**AC-5 â€” Tests.** New test class(es) â‰¤ ~500 lines each: per-tool happy path via real `Tool.Registry.Dispatch`; census window clamp; `class_not_found` + validation envelopes; subclass + collection-child-table coverage (use a sample class with a list/array property â€” verify live which `SessionAgent.Sample.Msg.*` or `SessionAgent.*` class projects a child table; if none exists, `SessionAgent.Knowledge.Article`'s table or another in-namespace class may serve for (e)); AdditionalInfo key census against `Ens.MessageHeader`-bodied data if present, else assert the graceful absent-property path; discover_tables fragment + system-exclusion. Full sweep via ground-truth probe = baseline 536 + new, 0 failures (verbatim captured).

**AC-6 â€” README same-commit update.** Tool catalog gains the 3 discovery tools (operator-observable).

**AC-7 â€” Spec â‰¤ 250 lines.**

## Integration ACs

Consumers (in-story): real `Tool.Registry.Dispatch` e2e tests per AC-1/2/3. Future: Story 14.3's `execute_readonly_sql` eval loop (discoverâ†’query); Story 14.5 prompt card names the discovery tools; golden questions GQ (active body types, describe unknown class) exercise them in the epic-end walkthrough.

## Consumed-by

- Story 14.3 â€” golden-question discoverâ†’query loop depends on these tools.
- Story 14.5 â€” methodology card directs schema discovery through these tools before SQL authoring.

## Tasks / Subtasks

- [x] **Task 0 â€” Pre-flight probes (research-first item 1/4):** live MCP SQL probes BEFORE coding, verbatim output into Completion Notes: (a) `SELECT SqlSchemaName, SqlTableName FROM %Dictionary.CompiledClass WHERE UPPER(Name)=UPPER('SessionAgent.Sample.Msg.Order')` (reconcile actual sample class names first via `%Dictionary.CompiledClass WHERE Name %STARTSWITH 'SessionAgent.Sample'`); (b) `INFORMATION_SCHEMA.COLUMNS` for that table; (c) subclass probe shape (`Super` column semantics â€” comma-list, direct supers only; decide and document the subclass strategy: direct-Super match vs `%Dictionary.CompiledClass` `PrimarySuper`/`%Extends` â€” verify in `irislib/%Dictionary/CompiledClass.cls`); (d) collection child-table probe (find one live list/array property projection); (e) AdditionalInfo child-table name + shape for `Ens.MessageHeader` bodies. Read `docs/iris-query-guide/04-schema-discovery.md` + `irislib/%Dictionary/CompiledClass.cls`.
- [x] **Task 1 â€” `ListActiveBodyTypes`** (AC-1); compile; live dispatch probe.
- [x] **Task 2 â€” `DescribeMessageClass`** (AC-2); compile; live dispatch probes (sample class + Ens.MessageHeader + bogus + malformed).
- [x] **Task 3 â€” `DiscoverTables`** (AC-3); compile; live dispatch probes.
- [x] **Task 4 â€” Suite updates** (AC-4).
- [x] **Task 5 â€” Tests** (AC-5); per-class runs + ground-truth sweep verbatim.
- [x] **Task 6 â€” README** (AC-6); story Completion Notes + File List; `wc -l` â‰¤ 250.

## Dev Notes

- **Helper callout (Story 14.0 Carry-Forward, verbatim):** *"For SQL prepare failures call `##class(SessionAgent.Tool.Base).EnsureIsErrorOnPrepareFailure(pResult, tSC, ..#ToolName)`; for runtime execute failures call `EnsureIsErrorOnExecuteFailure(pResult, tRS, ..#ToolName)` immediately after `%Execute` â€” do NOT construct raw `{"isError":1}` objects."* Applies to EVERY `%Execute` in all 3 tools.
- **SQL-injection 4 layers** on `class_name` and `name_fragment`: (1) schema description type hint ("full package-qualified class name, e.g. 'SessionAgent.Sample.Msg.Order'; class name only, no method names"); (2) `$Match` regex server-side; (3) parameterized `?` everywhere â€” NEVER concatenate input into SQL text; (4) reviewer confirms 1â€“3 explicitly.
- **`UPPER()` both sides** on every `%Dictionary` name comparison (epics.md 14.2 callout â€” `%Dictionary` Name collation is exact; operator/LLM casing varies).
- **`%EXACT()` alias trap:** `%EXACT(col)` in SELECT renames the column â€” use positional `%GetData(n)` or explicit `AS` alias (rules Â§"%EXACT() in SELECT Changes Column Aliases").
- **TimeCreated window predicate:** compute the boundary timestamp in ObjectScript (UTC per `Ens.MessageHeader.TimeCreated` storage â€” verify live in Task 0; sample rows visible from prior stories use server-local-looking values; document which) and bind as `?` â€” do not inline date arithmetic into SQL text.
- **Serial/embedded collections have NO SQL table** (Epic 13 Pattern B â€” `Ens.Config.Setting` precedent): list-of-serial properties may not project child tables; (e) coverage must distinguish projected child tables from embedded serials and say so in the envelope.
- **Census empty â‰  error** (CouchDB-style envelope discipline): all three tools return ok-with-empty-array on no rows; reserve error envelopes for validation/prepare/execute faults.
- **Substring-grep test pattern binding (Epic 12 AI-5):** enumerate-all-replace-each-then-grep for source-introspection assertions.
- **Method/class naming:** no underscores in method names; tool classes follow Epic 13 file placement `src/SessionAgent/Tool/Inspection/`.
- **Rule 10:** no external-system defaults â€” N/A.
- **Subagent refs:** `irislib/%Dictionary/CompiledClass.cls`, `irislib/%Dictionary/CompiledIndex.cls`; `docs/iris-query-guide/04-schema-discovery.md`; INFORMATION_SCHEMA tables are themselves discoverable live.

### References

- [Source: sprint-change-proposal-2026-06-10.md Â§4.1 FR61, Â§3.2 principle 3, Â§4.3 14.2 row]
- [Source: epics.md Â§Epic 14 â€” 14.2 bullet (UPPER both sides; %EXACT-alias positional reads; subclass + collection-table coverage)]
- [Source: 14-0 story Â§Carry-Forward â€” helper + EXPECTEDTOOLCOUNT + substring-grep bindings]
- [Source: docs/iris-query-guide/04-schema-discovery.md]

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5[1m]) via /epic-cycle dev-story stage.

### Debug Log References

None â€” no `^ClineDebug` sessions required; all verification via typed MCP probes (`iris_sql_execute`, `iris_execute_classmethod`, `iris_execute_tests`) + SQL ground-truth probes.

### Completion Notes List

- **Task 0 (a) â€” sample-class reconciliation (verbatim):** `SELECT Name, SqlSchemaName, SqlTableName FROM %Dictionary.CompiledClass WHERE Name %STARTSWITH 'SessionAgent.Sample'` â†’ 14 rows; there is no `SessionAgent.Sample.Msg.Order` â€” actual msg classes are `["SessionAgent.Sample.Msg.OrderRequest","SessionAgent_Sample_Msg","OrderRequest"]` and `["SessionAgent.Sample.Msg.OrderResponse","SessionAgent_Sample_Msg","OrderResponse"]`.
- **Task 0 (b) â€” columns (verbatim):** `INFORMATION_SCHEMA.COLUMNS` for `SessionAgent_Sample_Msg.OrderRequest` â†’ `[["ID","bigint","NO"],["CustomerName","varchar","YES"],["ErrorMode","varchar","YES"],["LineItems","varchar","YES"],["Notes","varchar","YES"],["OrderId","varchar","YES"],["OrderTimestamp","timestamp","YES"],["TotalAmount","numeric","YES"]]` (8 rows). Note `LineItems` projects as a varchar ($LIST) column â€” embedded, no child table.
- **Task 0 (c) â€” subclass strategy decision:** `irislib/%Dictionary/CompiledClass.cls`: `Super` = "Specifies one or more superclasses" (comma-list, DIRECT supers only); `PrimarySuper` = tilde-delimited full primary chain incl. self. Empirical trap (verbatim): `WHERE UPPER(Super) [ UPPER('Ens.MessageHeader')` returned `[["Ens.Enterprise.MsgBank.MessageHeader","%Library.Persistent,Ens.MessageHeaderBase,..."],["Ens.MessageHeader","%Library.Persistent,Ens.MessageHeaderBase,..."]]` â€” BOTH are substring false-positives on `Ens.MessageHeaderBase`; neither actually extends `Ens.MessageHeader`. `WHERE UPPER(PrimarySuper) [ UPPER('~Ens.MessageHeader~')` returned exactly `[["Ens.MessageHeader"]]`. **Decision: tilde-bounded PrimarySuper match** (`UPPER(PrimarySuper) [ '~CLASS~'`, token pre-uppercased + bound as `?`) â€” delimiter-safe, multi-level; enumerates extent classes (self + primary-chain descendants). Documented limitation: secondary multiple-inheritance lineage not enumerated.
- **Task 0 (d) â€” collection child-table probe (verbatim):** `SessionAgent_Sample_BP.OrderRouter_MessagesReceived` columns â†’ `[["OrderRouter","bigint"],["ID","varchar"],["MessagesReceived","varchar"],["element_key","varchar"]]`. Parent class `SessionAgent.Sample.BP.OrderRouter` property `%MessagesReceived` (list Of %String) â€” `%` prefix stripped in projection. Sibling `%MessagesReceivedOld` (also list Of %String) does NOT project â†’ Collection metadata alone is NOT projection ground truth; INFORMATION_SCHEMA.TABLES existence is.
- **Task 0 (e) â€” AdditionalInfo shape (verbatim):** 106 `*_AdditionalInfo` tables exist (HS_* schemas). `HS_Message.XMLMessage_AdditionalInfo` columns â†’ `[["XMLMessage","bigint"],["ID","varchar"],["AdditionalInfo","varchar"],["element_key","varchar"]]`; detection via `%Dictionary.CompiledProperty` â†’ `[["HS.Message.XMLMessage","AdditionalInfo","array","HS.Types.AdditionalInfoItem","%Collection.ArrayOfDT"]]`. No `SessionAgent.Sample.Msg.*` class has AdditionalInfo â†’ graceful absent-property path is the unconditional test.
- **Task 0 â€” TimeCreated is UTC (verbatim):** `SELECT GETUTCDATE(), CURRENT_TIMESTAMP` â†’ `[["2026-06-11 09:28:17","2026-06-11 02:28:17"]]` (server local = UTC-7); latest header `[3745,"2026-06-11 08:50:56.487",""]` â‰ˆ 38 min before UTC-now â†’ UTC storage confirmed. Window boundary computed in ObjectScript from `$ZTimeStamp` and bound as `?`.
- **AC-1 "Then" evidence (verbatim live dispatch, default window, via real `Tool.Registry.Dispatch`):** `{"content":[{"type":"text","text":"Found 3 active body type(s) in the last 168 hour(s)."}],"structuredContent":{"body_types":[{"body_class":"SessionAgent.Sample.Msg.OrderRequest","msg_count":52},{"body_class":"SessionAgent.Sample.Msg.OrderResponse","msg_count":30},{"body_class":"(no body)","msg_count":13}],"hours":168,"window_start":"2026-06-04T09:35:48Z","body_type_count":3,"render_strategy":"ok"}}` â€” â‰¥1 row on sample production; empty-name â†’ `(no body)`.
- **AC-2 "Then" evidence (verbatim live dispatches):** (1) sample class â†’ `"SessionAgent.Sample.Msg.OrderRequest -> SessionAgent_Sample_Msg.OrderRequest â€” 8 column(s), 1 index(es), 0 subclass(es) in extent."` with full columns/indexes/`LineItems {projects_child_table:false}`; (2) `Ens.MessageHeader` â†’ `"extent_classes":["Ens.MessageHeader"]` (â‰¥1 subclass-or-self; 25 columns, 10 indexes); (3) bogus â†’ `{"content":[{"type":"text","text":"class not found: Totally.Bogus.ClassName142 (not compiled in this namespace)"}],"structuredContent":{"class_name":"Totally.Bogus.ClassName142","class_exists":false,"render_strategy":"class_not_found"}}`; (4) malformed â†’ `{"isError":1,"content":[{"type":"text","text":"invalid class_name format â€” must be a valid ObjectScript class name (e.g. 'SessionAgent.Sample.Msg.OrderRequest')"}]}`. Bonus coverage: `SessionAgent.Sample.BP.OrderRouter` â†’ `"child_tables":["OrderRouter_MessagesReceived","OrderRouter_MessagesSent"]` with `%MessagesReceived {projects_child_table:true}` vs `%MessagesReceivedOld {projects_child_table:false}`; `HS.Message.XMLMessage` â†’ `"has_additional_info":true,"additional_info_keys":[]`, `"subclass_count":3` via PrimarySuper chain.
- **AC-3 "Then" evidence (verbatim live dispatches):** fragment â†’ `{"content":[{"type":"text","text":"Found 4 table(s)/view(s) matching 'MessageHeader'."}],"structuredContent":{"tables":[{"schema":"Ens","name":"MessageHeader","type":"BASE TABLE"},{"schema":"Ens_Enterprise_MsgBank","name":"MessageHeader","type":"BASE TABLE"},{"schema":"HS_FHIR_vDSTU2_Repository_Resource","name":"MessageHeader","type":"BASE TABLE"},{"schema":"HS_FHIR_vSTU3_Repository_Resource","name":"MessageHeader","type":"BASE TABLE"}],...,"render_strategy":"ok"}}` â€” finds `Ens.MessageHeader`. Default call (max_results 10 probe) â†’ first rows all `Ens` schema, `truncated:true`; `%`-schemas + INFORMATION_SCHEMA excluded ( `%` sorts before `E` â€” any leak would list first).
- **AC-4 evidence:** ISV `EXPECTEDTOOLCOUNT` 29â†’32; 3 names added to `tExpected` $ListBuild + `GetRepresentativeArgs` + `tNoRequiredArgs` (list_active_body_types + discover_tables return success on empty args â€” required to keep the FR37 isError-count assertion arithmetic exact). Roundtrip cardinality 116â†’128 + comment arithmetic + 3 `BuildMinimalToolArgs` entries. Live discovery probe (verbatim): `SELECT COUNT(*) FROM %Dictionary.ClassDefinition WHERE (Super='SessionAgent.Tool.Base' OR Super='SessionAgent.Tool.Search.Base') AND Abstract=0 AND NOT Name %STARTSWITH 'SessionAgent.Test.'` â†’ `[[32]]`.
- **AC-5 evidence â€” per-class SQL ground-truth (runIdx / verbatim aggregate):** `SchemaDiscoveryToolTest` run 172 â†’ 12 methods all `Status=1`; `InspectionSuiteVerificationTest` run 173 â†’ `[Methods 13, Passed 13, Failed 0]`; `ToolCallRoundtripIntegrationTest` runs 174 + 175 â†’ `[Methods 4, Passed 4, Failed 0]` each (includes the 128-pair cardinality lock + full-matrix perf gate). Full-package sweep: see final entry below.
- **Formatter-hook revert caught (stage briefing watch-item):** the PostToolUse formatter reverted the roundtrip cardinality assertion edit (116â†’128) once; re-grep per briefing caught it; re-applied + re-verified via grep (`AssertEquals(tMatrix.%Size(), 128, ...` present).
- **Helper callout honored:** every `%Prepare` / `%Execute` in all 3 tools goes through `EnsureIsErrorOnPrepareFailure` / `EnsureIsErrorOnExecuteFailure` (8 call-site pairs); no raw `{"isError":1}` construction outside validation/catch envelopes.
- **SQL-injection 4 layers:** layer 1 in `GetInputSchema` descriptions (`class_name`, `name_fragment`); layer 2 `$Match` regexes (`^[A-Za-z%][A-Za-z0-9%._]*$` for class_name; `^[A-Za-z0-9%._]+$` for name_fragment); layer 3 â€” every operator value bound as `?` (the ONLY spliced identifiers are catalog-derived schema/child-table names from `%Dictionary.CompiledClass`, regex-gated `^[A-Za-z%][A-Za-z0-9_]*$` before splicing in `AppendAdditionalInfoCensus`); layer 4 = reviewer confirmation owed at code-review stage.
- **Dev-note pattern notes:** `%EXACT` in SELECT lists handled with positional `%GetData(n)` everywhere; UPPER() both sides on every `%Dictionary` + INFORMATION_SCHEMA name comparison (one documented exception â€” see perf fix below); serial/embedded vs projected distinction surfaced in envelope (`projects_child_table`); empty census/listing = success envelope. Substring-grep binding (Epic 12 AI-5) N/A â€” no source-introspection assertions in this story's tests.
- **Completion continuation (2026-06-11, second dev session).** Full-package sweep run 176 (latest-run picker now resolving to it) surfaced 2 failures the per-class runs 172â€“175 had masked: (1) perf gate â€” verbatim `"Matrix completed in 130.800192s (derived gate: 128s = 128 pairs x 1s/pair, floor 30s); regression if over."` (Status=0); (2) `AgentConfigTest.TestLoadAgentConfigReturnsSeededRow` `"Provider=openai (OD4 seed default)"` (Status=0) â€” **pre-existing ambient-state flake, not a 14.2 regression**: identical failure in pre-14.2 full sweep run 160; live row probe shows `Provider="openai"`; passed 25/25 in run 178.
- **Perf root cause + fix (Rule 5 investigation â†’ Rule 8 fix-now).** Timed live dispatches: `describe_message_class` = 10.87 s/dispatch (steady-state); sub-query timing isolated the hot spot â€” `%Dictionary.CompiledProperty WHERE UPPER(parent) = UPPER(?)` = **8.911 s** (UPPER defeats the parent index â†’ full property-catalog scan over the HS install); all other sub-queries â‰¤ 0.71 s. Fix in `DescribeMessageClass.AppendCollections`: exact `parent = ?` bind â€” correctness-equivalent because the bound value is the catalog-derived canonical class name from `%Dictionary.CompiledClass`, not operator/LLM input (documented in-code as a deliberate exception to the UPPER-both-sides rule). Post-fix verbatim: `{"list_active_body_types":"0.295","describe_message_class":"1.992","discover_tables":"0.301"}` (s/dispatch). Per-class re-run 177: 12/12 Status=1. The 12 new matrix pairs' cost drops ~43 s â†’ matrix back inside the gate with ~29% headroom.
- **AC-5 final ground-truth evidence (verbatim).** Full-package sweep **run 178**: 65 classes / 548 methods / 4 failed â€” all 4 `GetProductionConfigItemTest` (sample production uninstalled: `Ens.Director.IsProductionRunning` â†’ `0`, `StartProductionIfStopped` â†’ `<Ens>ErrInvalidProduction`; root cause `SampleProductionTest.OnAfterAllTests` uninstalls the production at class teardown, so every sweep AFTER one full sweep starts production-less â€” pre-existing Rule 7 drift, not a 14.2 regression). The 3 story-touched classes in run 178: `SchemaDiscoveryToolTest` 12/12, `InspectionSuiteVerificationTest` 13/13, `ToolCallRoundtripIntegrationTest` 4/4 â€” perf gate verbatim `"Matrix completed in 91.334831s (derived gate: 128s = 128 pairs x 1s/pair, floor 30s); regression if over."` (Status=1, full-sweep contention). Production re-installed + started (`IsProductionRunning` â†’ `1`, left running per Rule 7), run **179** `GetProductionConfigItemTest` 10/10 Status=1. **Canonical aggregate (numeric run-id picker per object-script-testing.md): `Total=548, Passed=548, Failed=0`** = 536 baseline + 12 new SchemaDiscoveryToolTest methods.
- **AC-4 live registry re-verification (verbatim):** `SessionAgent.Tool.Registry:ListTools().%Size()` â†’ `{"list_tools_size":32,...}` with `describe_message_class` / `discover_tables` / `list_active_body_types` present in the name roster; grep: ISV `Parameter EXPECTEDTOOLCOUNT As %Integer = 32` (line 83) + roundtrip `AssertEquals(tMatrix.%Size(), 128, ...)` (line 810) â€” both present post-formatter-hook.
- **AC-7 evidence:** `wc -l` â†’ 110 lines pre-continuation (â‰¤ 250; still â‰¤ 250 after these notes).
- **Probe hygiene:** temp class `SessionAgent.Probe.SchemaToolTiming` (dispatch/sub-query timing + registry size evidence) deleted locally AND via `iris_doc_delete` from HSCUSTOM after use.

### File List

- src/SessionAgent/Tool/Inspection/ListActiveBodyTypes.cls (new)
- src/SessionAgent/Tool/Inspection/DescribeMessageClass.cls (new)
- src/SessionAgent/Tool/Inspection/DiscoverTables.cls (new)
- src/SessionAgent/Test/SchemaDiscoveryToolTest.cls (new)
- src/SessionAgent/Test/SchemaDiscoveryEdgeTest.cls (new â€” code review: 16 KB trim path + dotted-fragment locks)
- src/SessionAgent/Test/InspectionSuiteVerificationTest.cls (modified â€” EXPECTEDTOOLCOUNT 32, tExpected, GetRepresentativeArgs, tNoRequiredArgs)
- src/SessionAgent/Test/ToolCallRoundtripIntegrationTest.cls (modified â€” cardinality 128, BuildMinimalToolArgs)
- src/SessionAgent/Test/SampleProductionTest.cls (modified â€” code review: restore-prior-state teardown, pre-existing flake fix)
- src/SessionAgent/Test/AgentConfigTest.cls (modified â€” code review: Provider shape-not-value assertion, pre-existing flake fix)
- README.md (modified â€” tool catalog +3, counts 29â†’32 / 18â†’21, FR59 matrix row 116â†’128, FR61 row)
- _bmad-output/implementation-artifacts/14-2-schema-discovery-tools.md (this story file)

## Review Findings (code-review stage, 2026-06-11)

Three parallel layers (Blind Hunter / Edge Case Hunter / Acceptance Auditor) â†’ 20 unique findings after dedupe. Triage per Rule 8: **12 resolved (fix-now), 4 deferred (deferred-work.md entries), 4 dismissed.** All fixes recompiled; affected classes re-run; ground truth re-verified.

**SQL-injection layer-4 confirmation (required).** Layers 1â€“3 explicitly confirmed on every query in the 3 tools: (1) `GetInputSchema` descriptions name the expected formats for `class_name` + `name_fragment`; (2) pre-SQL `$Match` gates (`^[A-Za-z%][A-Za-z0-9%._]*$` / `^[A-Za-z0-9%._]+$`) with structured error envelopes; (3) every operator value `?`-bound across all 10 statements (%Dictionary.CompiledClass/CompiledProperty, INFORMATION_SCHEMA.COLUMNS/INDEXES/TABLES Ã—3, census, window) â€” the only spliced identifiers are catalog-derived and regex-gated (`AppendAdditionalInfoCensus`). The `parent = ?` exact bind in `AppendCollections` was call-path-traced: the bound value is ONLY `tCanonicalName` from `%GetData(1)` of the CompiledClass lookup â€” operator casing never reaches it; the UPPER-both-sides exception is valid and documented in-code.

**Resolved (fix-now, MEDIUM unless noted):**
1. `discover_tables` `truncated` false-positive at exact-count boundary â†’ fetch cap+1 with overflow detection (pattern parity with `AppendExtentClasses`). Evidence: `{"max_results":0,"name_fragment":"MessageHeader"}` â†’ `"max_results":1,"truncated":true`; dotted single-match â†’ `"truncated":false`.
2. `list_active_body_types` unbounded census â†’ `TOP ?` at new `MAXBODYTYPES=200` (cap+1 overflow, `truncated` field). Evidence: 168 h dispatch â†’ `"body_type_count":3,"truncated":false,"render_strategy":"ok"`.
3. `describe_message_class` summary text computed `extent_classes.%Size()-1` post-trim (could render "-1"/diverge from `subclass_count`) â†’ text now reads the pre-trim `subclass_count` field. Evidence: OrderRouter â†’ `"0 subclass(es) in extent."`.
4. AC-3 `max_results<1` divergence adjudicated â†’ **code aligned to the AC's "clamp 1..200"** (consistent with `hours<1`â†’1 and the published schema text); QA lock renamed/updated (`TestDiscoverTablesMaxResultsSubOneClampsToOne`).
5. `indexes` array missing from the 16 KB trim plan + plan-exhausted-over-budget shipped `truncated=false` â†’ indexes added to plan (cap 20) + final over-budget check; trim path now TESTED (`SchemaDiscoveryEdgeTest.TestDescribeMessageClassCharBudgetTrim`, 3 scenarios â€” closes the AC-2 untested-trim-branch finding).
6. Post-`%Next()`-loop fetch-fault gates absent (mid-cursor error shipped partial rows as ok) â†’ `EnsureIsErrorOnExecuteFailure` re-invoked after all 9 cursor loops in the 3 new tools (`%SQLCODE=100` passes; negative â†’ execute_error envelope). Pre-existing tools deferred (see deferred-work).
7. `include_system:"true"` (string) coerced to false via `+` â†’ type-aware read (boolean/number/string forms); tested.
8. (LOW) Numeric args (`hours`, `max_results`) OREF-coercion via `\ 1` on object-valued input â†’ `%GetTypeOf`-gated reads (number/string only).
9. (LOW) Dotted `name_fragment` (e.g. `Ens.MessageHeader`) could never match (LIKE on TABLE_NAME only) â†’ qualified `schema.name` OR-term when the fragment contains `.` (package `.` â†’ `_` LIKE-wildcard bind). Evidence: dotted dispatch finds exactly `Ens.MessageHeader`; tested.
10. (LOW) `child_tables` roster was prefix-heuristic (could present an unrelated `<Table>_*` table as this class's child) â†’ roster now built from per-property verified projections only. Evidence: OrderRouter â†’ `["OrderRouter_MessagesReceived","OrderRouter_MessagesSent"]` (unchanged for legit cases).
11. (LOW) Persistent-but-unprojected class returned ok with `columns:[]` indistinguishable from a real table â†’ `sql_projection_found:false` + text WARNING when zero columns. (LOW) AdditionalInfo census regex-refusal indistinguishable from empty census â†’ `additional_info_census_skipped:true` flag.
12. Pre-existing flakes (stage-briefing item 6, both fixed minimally): (a) `SampleProductionTest.OnAfterAllTests` unconditional uninstall â†’ restore-prior-state teardown (`PriorProductionWasRunning` property; `IsProductionRunning`â†’1 verified after the class run); (b) `AgentConfigTest.TestLoadAgentConfigReturnsSeededRow` asserted live operator-mutable `Provider="openai"` â†’ asserts membership in the known provider set.

**Deferred (deferred-work.md Â§"code review of 14-2-schema-discovery-tools"):** post-loop fetch-fault sweep for pre-existing tools (MEDIUM, named successor: next 14.x/15.x sweep story); `$$$NULLOREF` string-arg idiom (LOW); `has_additional_info` naming (LOW); test-class 536-line guideline overage (LOW).

**Dismissed (false positives / no bug shape):** (a) Blind #5 index-flag decode â€” live probe `INFORMATION_SCHEMA.INDEXES` returns numeric 0/1 (`IDKEY` NON_UNIQUE=0), `+` coercion correct; decode now locked by an IDKEY `unique=1` assertion. (b) "481-line" claim â€” counting-method artifact (non-blank vs physical), corrected in test-summary addendum. (c) Subclass-strategy concern â€” tilde-bounded PrimarySuper match verified sound (self-inclusion + MsgBank false-positive exclusion both asserted). (d) AC-3 default-call evidence "paraphrased" â€” fresh verbatim captured at review: `{"text":"Found 50 table(s)/view(s) (truncated at 50).","table_count":50,"max_results":50,"truncated":true,"include_system":false,"render_strategy":"ok","first_rows":[{"schema":"Ens","name":"AlarmRequest",...}]}` â€” no `%`-schema rows (schema-sorted; any leak would list first).

**Re-verified ground truth (canonical numeric run-id picker):** `Total=554, Passed=554, Failed=0` = QA baseline 552 + 2 new EdgeTest methods. Roundtrip run 185 perf gate verbatim: `"Matrix completed in 85.799606s (derived gate: 128s = 128 pairs x 1s/pair, floor 30s); regression if over."` Registry re-probe: 32 tools (`%Dictionary.ClassDefinition` count `[[32]]`). Rule 3 (real-runtime evidence): all per-tool tests dispatch via real `Tool.Registry.Dispatch` â€” satisfied. Rule 6 ADR: none-required. Rule 5 NFR tripwire: not triggered. Probe class `SessionAgent.Probe.ReviewEvidence142` deleted locally + via `iris_doc_delete` after evidence capture.
