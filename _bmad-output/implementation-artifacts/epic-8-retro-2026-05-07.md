# Epic 8 Retrospective — Search Agent Foundation

**Date:** 2026-05-07
**Epic:** Epic 8 — Search Agent Foundation (Stories 8.0–8.7)
**Status:** Complete; retrospective marked `done`.
**Lead:** Claude Opus 4.7 (1M context)
**Project Lead:** Josh

## Epic summary

| Metric | Value |
|---|---|
| Stories completed | 8/8 (Story 8.0 cleanup carrier + 8.1 vocabulary schemas + 8.2 invariant test + 8.3 six indexed-access tools + 8.4 super-session + 8.5 body-field + 8.6 InspectBodyCandidates + 8.7 VocabLookup) |
| Reviewer auto-resolved fix-nows | Story 8.0: 1 MEDIUM (HTML markup in Description) · Story 8.1: 1 MEDIUM (test cleanup uses `%KillExtent()`) · Story 8.2: 1 LOW · Story 8.3: 1 MEDIUM (orphan-audit-row sweep cap) · Story 8.4: 1 MEDIUM (count discrepancy correction) · Story 8.5: 0 (1 MEDIUM deferred to 8.6 with Rule 9 binding) · Story 8.6: 0 · **Story 8.7: 2 HIGH + 1 MEDIUM** (description-input drop, save-mode probe schema drift, hardcoded list limit) |
| User-led empirical battery findings | 1 LLM-side discoverability gap (`inspect_body_candidates` passed unqualified `"OrderRequest"` instead of full package-qualified `"SessionAgent.Sample.Msg.OrderRequest"`); 1 welcome-message capability enumeration drift (chat panel still says "I'm an inspection agent" — doesn't mention search) |
| Final regression sweep | **326/326 PASS via SQL ground-truth probe (numerical-MAX-of-non-empty-run form)** — Epic 7 close baseline 291 + 35 new test methods across Stories 8.1–8.7 |
| Bounded-WHERE invariant | "10 production search tool(s) discovered; 0 violation(s)" — Epic 8 acceptance gate met |
| End-to-end live walkthrough | ✅ All 10 search tools dispatched via direct backend invocation against real production data; 3 search tools (`search_by_status`, `inspect_body_candidates`, `vocab_lookup`) dispatched end-to-end via Chrome-DevTools chat-panel integration test; audit-log SQL probe confirms `(SessionAgent, VocabWrite, explicit)` triple correctly written |
| Mgmt Portal observability | ✅ Task ID 1009 `SessionAgent.PurgeOrphanedChatHistory` Description column now plain English (Story 8.0 AC-2 fix-now confirmed via `iris_task_list`) |
| Tool.Registry tool count | ✅ 23 tools (13 inspection + 10 search) registered post-Story-8.3 filter widen |
| Cycle calendar duration | Started 2026-05-07; closed 2026-05-07 (single-day cycle) |
| Project velocity | 8/10 epics complete (80%) — 8 calendar days from project start |

## What went well (user-confirmed; all 4 wins accepted as the headline summary)

| # | Theme | Evidence |
|---|---|---|
| W-1 | **Rule 9 carry-forward shipped two-deep cleanly.** | Story 7.0 → Story 8.0 → Story 9.0 chain held for items E/H/I (rebound). MEDIUM-8.5-F02 bound from Story 8.5 to Story 8.6 closed cleanly via the 3-coordinated-changes refactor (`Tool.Search.Base.BuildBoundedWhereClause` alias-param + invariant-test extension + `SearchByBodyField` retrofit). First time the named-successor pattern shipped two-deep within a single epic AND closed an inherited binding inline. |
| W-2 | **Step-1-time production Bootstrap eliminated mid-story detours.** | Per Story 8.0 AC-4 codification (Rule 7 sub-clause sharpening from Epic 7 retro AI-4): the lead Bootstrapped the sample production at /epic-cycle Step 1 BEFORE any per-story dispatch. **Zero mid-story re-Bootstrap detours across all 8 stories.** First epic in 5+ where this didn't happen. The codification + Step-1 enforcement pattern works as designed. |
| W-3 | **Reviewer caught HIGH bugs in the largest story (8.7).** | Story 8.7 reviewer auto-fixed 2 HIGH (description-input silently dropped; save-mode probe drifted from schema's pair-unique index — would have caused second save with different MessageBodyClass to hit pair-unique violation) + 1 MEDIUM (hardcoded list limit). The dev/review separation pattern earned its keep at the most cognitively-loaded story of the epic. |
| W-4 | **Tool.Registry filter widen was the highest-leverage single fix.** | Story 8.3's reviewer-flagged predicted-bug (`Super = 'Tool.Base'` excluded `Tool.Search.Base` subclasses) was a 1-line SQL change. Without it, Stories 8.3–8.7's 10 search tools would have shipped invisible to `Tool.Registry.ListTools` → invisible to AgentLoop.Step5 → invisible to the LLM. The walkthrough's chat-panel integration test (where the LLM dispatched `search_by_status` / `inspect_body_candidates` / `vocab_lookup` from the existing Inspection chat panel) is direct evidence the widen was load-bearing. |

## What didn't go well (user-selected challenge)

| # | Theme | Severity | Evidence |
|---|---|---|---|
| C-1 (user-selected) | **Pre-retro empirical battery was insufficient — user-redirect for comprehensive walkthrough still required (4th consecutive cycle).** | MEDIUM (process discipline) | Lead emitted the 4-bullet pre-retro checklist verbatim per Story 8.0 AC-3 sub-clause (Task Manager probe + audit-event verification + rich-data live exercise + SQL ground-truth probe). Then proposed the retrospective question. User redirected: *"Before retro, please do a comprehensive test of all functionality of the system (systematically working through the completed stories) using Chrome-DevTools when possible."* Same redirect shape as Epic 1 / Epic 2 / Epic 7. The 4-bullet checklist captures the structural minimum but the user wants per-story walkthrough — especially with Chrome-DevTools where applicable. The codification works as far as it goes; it just doesn't go far enough. **Fix:** restructure the lead's pre-retro behavior so the per-story walkthrough is a mandatory pre-retro step, not an afterthought. See AI-1 below. |

## Other findings (lower-priority, codified but not user-selected as headline)

These were surfaced by the lead's analysis but the user did not select them as Epic 8's headline challenges. Captured as action items below.

| # | Theme | Severity | Evidence |
|---|---|---|---|
| C-2 | **Lex-MAX SQL probe form re-discovered 3 times.** | LOW (reviewer-caught each time) | Story 8.0 codified the fix (`MAX($PIECE(ID,'||',1)+0)` numeric-run-id form). Stories 8.2 / 8.4 / 8.6 each had the dev re-discover the lex-sort fragility (count off by hundreds because `'9||...' > '99||...'` lex-compare). Reviewers caught and corrected each time. **Codification didn't fully prevent re-discovery — devs reach for the simplest `MAX(ID)` form first.** |
| C-3 | **Welcome-message capability enumeration drift on the Inspection chat panel.** | LOW (operator-observable; not a code defect) | Walkthrough confirmed the LLM has access to all 23 tools (Story 8.3 widen) but the chat panel's welcome message still reads *"I'm an inspection agent for this session — I can read message headers, bodies, event and rule logs, BP source and runtime state, related sessions, and explain IRIS error codes."* No mention of search capability. Same shape as Epic 3 retro AI-15 (welcome message stale-reference). |
| C-4 | **LLM-side discoverability gap — unqualified class names.** | LOW (LLM-side; not a tool defect) | Walkthrough's `inspect_body_candidates` invocation: LLM passed `prefilter_value:"OrderRequest"` instead of full package-qualified `"SessionAgent.Sample.Msg.OrderRequest"`. Tool's exact-match semantic returned 0 matches faithfully. Epic 9's vocabulary digest will partially close this; tool descriptions can also explicitly demand full-qualified class names. |

## Epic 7 retro action-item follow-through — 5 of 5 honored

First epic in the project history where every prior commitment was discharged.

| Action item | Status | Evidence |
|---|---|---|
| AI-1 (Epic 7) — Codify operator-observable surface enumeration sub-section in Rule 4 | ✅ Completed in Story 8.0 AC-1 | Rule 4 sub-section appended; reviewer enforced via Story 8.7 catching the description-input drop HIGH finding |
| AI-2 (Epic 7) — Fix-now Installer Description propagation | ✅ Completed in Story 8.0 AC-2 | Task ID 1009 verified plain-text Description via `iris_task_list` MCP envelope (walkthrough) |
| AI-3 (Epic 7) — Codify Rule 6 pre-retro enforcement checklist | ✅ Completed in Story 8.0 AC-3 + first applied at THIS retro | Lead emitted the 4 bullets before proposing the retro question. **The checklist works AND is insufficient** (see C-1) — the user-redirect for walkthrough was still required. |
| AI-4 (Epic 7) — Sharpen Rule 7 Step-1-time probe | ✅ Completed in Story 8.0 AC-4 + applied at Step 1 of THIS cycle | Production was Bootstrapped at /epic-cycle Step 1; ZERO mid-story re-Bootstrap detours. **First epic in 5+** where this didn't happen. |
| Story 7.0 carry-forward E/G/H/I | E ✅ rebound to 9.0 / G ✅ closed in Story 8.0 / H ✅ rebound to 9.0 / I ✅ rebound to 9.0 | Story 8.0 AC-8 task |

## Action items (durable; captured for Story 9.0 carry-forward)

5 action items grounded in Epic 8 incidents. Each is triaged for codification in Story 9.0 (Epic 8 deferred cleanup) per the Rule 9 binding-deferral pattern.

| # | Rule / Codification | Source incident | Mechanism |
|---|---|---|---|
| **AI-1** | **Sharpen Rule 6 enforcement: per-story functional walkthrough is a MANDATORY pre-retro step (5th bullet to Story 8.0 AC-3 sub-clause).** Codify: *"(e) the lead MUST invite the user to scope a per-story functional walkthrough — including Chrome-DevTools where applicable for any UI surface — and execute it BEFORE the retrospective question."* This converts the user-redirect-pattern observed across Epic 1 / Epic 2 / Epic 7 / Epic 8 into a structural pre-retro step. | C-1 (user-selected headline challenge). | Append the 5th bullet to Rule 6 sub-section "Pre-retro enforcement checklist (lead-self-blocking)" in `.claude/rules/epic-cycle-discipline.md`. Cite Epic 8 retro user-selected challenge as originating finding (4th recurrence; sharpened wording alone is insufficient — explicit walkthrough step is needed). |
| **AI-2** | **Lex-MAX SQL probe form: failing-CI rule-tweak.** Edit `.claude/rules/object-script-testing.md` §"SQL-probe-as-ground-truth" with a "**Reviewer enforcement**" stanza: any future story diff that uses the OLD `MAX(ID) GROUP BY %EXACT(Name)` form (without `$PIECE(ID,'||',1)+0` numeric extraction) is a MEDIUM-severity finding per Rule 8 (predicted-bug shape: latest-run picker undercounts). | C-2 (3 re-discoveries this epic; codification didn't prevent). | Append a "**Reviewer enforcement**" stanza to the §"SQL-probe-as-ground-truth" sub-section. The codified canonical form already shipped in Story 8.0 AC-5; this AI adds the reviewer-blocking enforcement to make the codification load-bearing. |
| **AI-3** | **Welcome-message capability enumeration drift — rewrite to directive form.** Replace the Inspection chat panel's welcome message in `src/SessionAgent/UI/ChatPanelAsset.cls` (or wherever the welcome string lives) from the current 6-capability enumeration to a directive form that doesn't enumerate runtime state: *"Ask anything about this session OR about other sessions across the system."* — let the LLM enumerate from the live tool list at runtime. Same anti-pattern fix that Story 4.0 / Epic 3 retro AI-15 applied to the system prompt. | C-3 (walkthrough surfaced operator-visible drift; LLM has access to all 23 tools but welcome message lies). | ~5-line edit to the welcome-message string in the chat panel asset class. Plus reviewer enforcement: any new chat-panel welcome-message edit that enumerates runtime state is HIGH-severity per Rule 8 (predicted-bug: drift will silently ship). |
| **AI-4** | **Tool descriptions: explicit full-qualified-class-name guidance.** Edit `inspect_body_candidates` and `search_by_message_class` `Description` parameters to explicitly say: *"For `MessageBodyClassName`, provide the FULL package-qualified class name (e.g., `SessionAgent.Sample.Msg.OrderRequest`, not `OrderRequest`)."* Closes the LLM-side discoverability gap surfaced in walkthrough; Epic 9 vocab digest will compound the fix. | C-4 (walkthrough caught LLM passing unqualified `"OrderRequest"` → 0 matches). | ~2-line edit per tool. The Description parameter is the LLM's primary signal for argument formatting. |
| **AI-5** | **Welcome-message + tool-description Rule 4 stale-reference scan added to Story 9.0 spec.** Story 9.0's Task 0 stale-reference scan MUST grep for capability-enumeration drift across `*.js` (UI assets), `*.cls` (Description parameters), `*.md` (README, planning artifacts) and update each reference in the same commit. Per Rule 4 watch-item from Story 4.0 / Epic 3 retro AI-15 — but applying it at the next-epic-cleanup carrier rather than waiting for an empirical bug to surface. | C-3 + C-4 share a common shape (operator-visible / LLM-visible enumeration drift); Rule 4 sub-section already captures this but Story 8.0 didn't proactively grep. | Story 9.0 lead's Task 0 grep target list extended. |

**Capture surfaces** (load-bearing for Story 9.0):
- **Rule files**: `.claude/rules/epic-cycle-discipline.md` Rule 6 sub-clause extension (AI-1); `.claude/rules/object-script-testing.md` §"SQL-probe-as-ground-truth" reviewer enforcement (AI-2).
- **`src/SessionAgent/UI/ChatPanelAsset.cls`** (or sibling): welcome-message rewrite (AI-3).
- **Tool class Description parameters**: `inspect_body_candidates`, `search_by_message_class` (AI-4).

## Continued deferrals (genuine Rule 8 passes from Epic 8)

- **Story 7.0 carry-forward items E / H / I** (originally bound at Story 8.0 → re-rebound to Story 9.0 per Story 8.0 AC-8) — all 3 still bound to Story 9.0 per Rule 9. Item I is now on its **second Rule 9 recovery** (Story 4.7 → Story 8.0 → Story 9.0); if Story 9.0 also can't carry it, the recovery note compounds (third-recovery binding required).
- **LOW-8.4-F01** (empty-envelope shape stability), **LOW-8.4-F02** (lex-MAX recurrence pattern — closed by AI-2 above).
- **LOW-8.6-F01 / F02 / F03** (stale doc-comment, dead PPG init scaffolding, integer-vs-display Status echo).
- **LOW-8.7-F01 / F02** (fragile dead-code arg-array packing in `InvokeList`; awkward error prose `got: ''` for empty mode).

All LOW deferrals are Rule 8 test-3 (pure cosmetic with no predicted-bug shape).

## Significant discoveries

**One — Rule 9 named-successor binding shipped two-deep within a single epic.** Story 8.6's MEDIUM-8.5-F02 closure (alias-param refactor) is the cleanest application of Rule 9 seen across Epics 1–8. The binding lived inside Story 8.5's reviewer-flagged deferred-work entry; Story 8.6's spec author grepped, incorporated, and the dev closed it via 3 coordinated changes (Base.cls + invariant-test + SearchByBodyField retrofit) without scope drift. Same pattern as Story 7.0 → Story 8.0's E/G/H/I rebind, but the **closure-within-epic** path is novel.

**Two — Step-1-time production Bootstrap is now load-bearing.** Epic 7 retro AI-4 codified the Rule 7 sub-clause; Epic 8 was the first epic where it actually fired. The Bootstrap at Step 1 ran cleanly + populated session 10892 + held throughout the cycle. Zero mid-story re-Bootstrap detours. The Rule 7 sub-clause is no longer a watch-item; it's a verified-effective enforcement.

**Three — Tool.Registry filter widen at Story 8.3 was the unsung leverage point.** A 1-line SQL change (`Super = 'Tool.Base' OR Super = 'Tool.Search.Base'`) wired all 10 search tools into the production agent loop. Without it, Epic 8 would have shipped 10 invisible tools — only programmatic dispatch via `iris_execute_classmethod` would have worked; the chat panel's LLM would have been blind to them. The walkthrough's chat-panel integration test (where the LLM dispatched `search_by_status` / `inspect_body_candidates` / `vocab_lookup` from the existing Inspection chat panel) is direct evidence the widen was load-bearing. **Catching this at Story 8.3's reviewer cycle saved Epic 10's Search Agent UI from being the bug-discovery surface.**

**Four — The user-redirect for comprehensive walkthrough recurred for the 4th cycle in a row.** Same shape across Epic 1 / Epic 2 / Epic 7 / Epic 8: lead emits whatever pre-retro checklist exists, user redirects to deeper walkthrough. The Story 8.0 AC-3 4-bullet codification works as far as it goes (the lead DID emit the bullets this time) but doesn't go far enough — the user wants per-story functional walkthrough with Chrome-DevTools where applicable. AI-1 above codifies a 5th bullet making the walkthrough a mandatory pre-retro step.

## Lessons applied from Epic 7 retro

| Epic 7 commitment | Status in Epic 8 |
|---|---|
| AI-1 — Codify operator-observable surface enumeration | ✅ Codified in Story 8.0 AC-1; **first applied** in Story 8.7 reviewer's HIGH catch (description-input drop) — codification immediately surfaced a real bug. |
| AI-2 — Installer Description propagation fix-now | ✅ Codified + verified via `iris_task_list` walkthrough (Task ID 1009). |
| AI-3 — Codify Rule 6 pre-retro enforcement checklist | ✅ Codified in Story 8.0 AC-3; **insufficient** per C-1 — user-redirect still required. AI-1 (this retro) sharpens to 5-bullet form. |
| AI-4 — Rule 7 Step-1-time probe sharpening | ✅ Codified + first applied; **first epic in 5+** with zero mid-story re-Bootstrap. |
| Story 7.0 carry-forward E/G/H/I | All addressed (G closed in 8.0 AC-5; E/H/I rebound to 9.0). |

**No missed lessons** — every Epic 7 commitment was honored in Epic 8. Velocity holding.

## Next epic preparation — Epic 9: Search Agent — Vocabulary Learning

**Scope:** 5 stories (9.1–9.5). `%OnAfterSave` non-recursion + `SynthesizeAlias` determinism Task-0 probes (9.1) + `UserVocabulary.RecordSuccess`/`RecordFailure` recursion-safe trigger (9.2) + `Search.VocabularyDigest.Build` (9.3) + `AgentLoop` first-user-message prefix injection + Anthropic prompt-cache preservation test (9.4) + `RecordClickThrough` ZenMethod hyperevent stub (9.5).

**Dependencies on Epic 8:**

- ✅ `UserVocabulary` schema (Story 8.1) — substrate verified via vocab_lookup save+list round-trip in walkthrough.
- ✅ `vocab_lookup mode='save'` audit-emit pattern — re-verified via SQL probe of `%SYS.Audit` filtered to `(SessionAgent, VocabWrite, explicit)` triple (walkthrough).
- ✅ Search tools registered in `Tool.Registry` — confirmed 10/10 via walkthrough; AgentLoop.Step5 picks them up in any chat panel.

**No blocking gaps.** Epic 9 stories can begin immediately.

**Story 9.0 — Epic 8 cleanup carrier (must run at /epic-cycle epic 9 Step 2):**

When `/epic-cycle epic 9` runs, Step 2 (Retrospective Review & Story X.0) MUST read this retrospective doc AND `deferred-work.md`. Inputs for Story 9.0:

- Epic 8 retro AI-1 codification: ~5 lines added to Rule 6 sub-section (5th bullet) in `epic-cycle-discipline.md` (per-story walkthrough mandatory pre-retro step).
- Epic 8 retro AI-2 codification: ~10 lines added to `object-script-testing.md` §"SQL-probe-as-ground-truth" (reviewer-blocking enforcement of numeric-run-id form).
- Epic 8 retro AI-3 fix-now: welcome-message rewrite in chat panel asset class (~5 lines).
- Epic 8 retro AI-4 fix-now: tool-Description tightening on `inspect_body_candidates` + `search_by_message_class` (~2 lines per tool).
- Epic 8 retro AI-5 process: stale-reference grep added to Story 9.0's Task 0.
- Story 7.0/8.0 carry-forward bindings (items E / H / I): 3 substantive deferred-work items expected in Story 9.0 per Rule 9.

Story 9.0 should fit cleanly under the Rule 1 250-line spec cap.

## Critical-path items before Epic 9 starts

- **No critical-path code items** beyond Story 9.0's carry-forward work.
- **Apply Rule 6 pre-retro enforcement checklist with new 5th bullet** (per Epic 8 AI-1) at Epic 9 close — the 5th bullet codifies that the per-story walkthrough is mandatory.
- **Apply Rule 7 Step-1-time probe** at /epic-cycle epic 9 Step 1 — the codification proved load-bearing this epic.
- **Apply SQL-probe-as-ground-truth reviewer enforcement** (per Epic 8 AI-2) for every "N/N test pass" claim per Epic 4 AI-1 + Epic 8 AI-2 (continues to hold for Epic 9).
- **Apply operator-observable surface enumeration** (per Epic 7 AI-1) for every Epic 9 story shipping a `%Persistent` class or operator-observable artifact.

## Significant discoveries (epic-update potential)

**No epic plan changes required.** Epic 9's plan is sound — Task-0 probes + UserVocabulary trigger + VocabularyDigest assembly + AgentLoop integration + ZenMethod hyperevent stub. The Story 8.7 vocab_lookup foundation (UserVocabulary schema verified via live save-list round-trip) directly supports Epic 9's first 4 stories. No story rewrites needed.

**Epic 8 walkthrough surfaced one minor scope-clarification for Epic 9:** the LLM-side unqualified-class-name discoverability gap (C-4) is partially within Epic 9 scope (vocabulary digest will provide aliases like `"OrderRequest"` → `"SessionAgent.Sample.Msg.OrderRequest"`). Story 9.3's `VocabularyDigest.Build` design should explicitly enumerate this as a use-case the digest closes.

## Readiness assessment

| Dimension | Status |
|---|---|
| Testing & quality | ✅ 326/326 sweep at Epic 8 close. 10 search tools verified end-to-end against real production data. 8 reviewer-caught HIGH/MEDIUM auto-fixed across the cycle. |
| Deployment | ✅ All 8 commits pushed; HEAD = `a8f0aa3`. |
| Stakeholder acceptance | ✅ N/A — single-stakeholder hobby project; Project Lead validated all 8 stories + comprehensive walkthrough including Chrome-DevTools chat-panel integration test. |
| Technical health | ✅ All Epic 8 classes compile clean; auto-sync working; 23 tools registered (13 inspection + 10 search); bounded-WHERE invariant 10/10 PASS. |
| Unresolved blockers | ✅ Zero. Walkthrough finding (LLM unqualified class name) is a discoverability gap bound to Epic 9 vocab digest + AI-4 Tool-Description tightening; NO code defect. |
| Sample production state | ✅ Step-1 Bootstrap held throughout cycle. Production still running with sample session 10892 + walkthrough-generated sessions. |
| Search Agent backend | ✅ All 10 tools dispatchable via `Tool.Registry.Dispatch`; bounded-WHERE invariant structurally enforced; vocabulary persistence + 10 HL7 seed templates ready for Epic 9 vocabulary-learning capture. |
| Audit-volume bound (NFR-SC4) | ✅ Same as Epic 7 — Option B cascade preserved; Story 8.7 added 1 audit-event triple consumer (`(SessionAgent, VocabWrite, explicit)`), no new triples. |
| Next-epic dependencies | ✅ Zero blockers. UserVocabulary substrate verified via walkthrough. |

**Epic 8 is fully complete and Epic 9 is unblocked.** PRD FR15–FR23 (search-agent backend) + NFR-S1 (L1 read-only across 10 tools) + NFR-P2/P3 (bounded-WHERE + ≤50 candidate cap) + Epic 8 acceptance gate all empirically met; Search Agent backend is operator-trusted for pilot rollout once Epic 10's UI ships.

## Commitments made

- 1 NEW Rule 6 5th-bullet sub-clause (AI-1: walkthrough mandatory pre-retro step) — Story 9.0.
- 1 NEW Rule §"SQL-probe-as-ground-truth" reviewer enforcement stanza (AI-2) — Story 9.0.
- 1 NEW chat panel welcome-message rewrite (AI-3) — Story 9.0.
- 1 NEW tool-Description full-qualified-class-name guidance (AI-4) — Story 9.0.
- 1 NEW process — stale-reference grep added to Story 9.0's Task 0 (AI-5) — Story 9.0.
- 3 carry-forward deferred-work items (E / H / I from Story 7.0 → Story 8.0) — Story 9.0 via Rule 9 binding.
- This retrospective doc available for Epic 9's mandatory retrospective-review gate.
- API keys for OpenAI / Anthropic / Gemini / Ollama instance retained in `Ens.Config.Credentials` for Epic 9+ if needed (Epic 9 is backend-only + AgentLoop integration — Anthropic prompt-cache test will exercise Anthropic credentials).

## Next steps

1. Mark `epic-8-retrospective: done` in `sprint-status.yaml` (this commit does it).
2. Optional pause. Epic 8 closes the Search Agent backend foundation: 3 vocabulary persistence schemas + 10 search tools + comprehensive test class + bounded-WHERE invariant. Project status: 8/10 epics complete in 8 calendar days; 2 epics remain (Search Agent vocabulary learning / portal subclass + UI embed).
3. Begin Epic 9 (Search Agent — Vocabulary Learning) when ready via `/epic-cycle epic 9`. Story 9.0 will pick up the 5 action items above + the 3 carry-forward deferred-work items per Rule 9 binding-deferral practice.

---

## Project Lead reflections (2026-05-07)

_Optional — invited but not yet captured. The lead pre-emitted the 4-bullet pre-retro empirical battery checklist (per Story 8.0 AC-3) and proposed the retrospective question. The user redirected: *"Before retro, please do a comprehensive test of all functionality of the system (systematically working through the completed stories) using Chrome-DevTools when possible."* That redirect drove a comprehensive backend systematic test (10 search tools against real production data) + a Chrome-DevTools chat-panel integration test (3 search tools dispatched end-to-end via the Inspection chat panel, citation chips rendered, audit-log SQL probe verifying the VocabWrite triple). The redirect surfaced one LLM-side discoverability gap (unqualified class names) bound to Epic 9 vocab digest + AI-4 tool-description tightening. The redirect itself is the headline lesson of this epic — the 4th consecutive cycle where it's been required, and the trigger for AI-1's Rule 6 5th-bullet codification._
