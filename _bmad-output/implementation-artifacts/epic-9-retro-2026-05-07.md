# Epic 9 Retrospective — Search Agent Vocabulary Learning

**Date:** 2026-05-07
**Epic:** Epic 9 — Search Agent Vocabulary Learning (Stories 9.0–9.5)
**Status:** Complete; retrospective marked `done`.
**Lead:** Claude Opus 4.7 (1M context)
**Project Lead:** Josh

## Epic summary

| Metric | Value |
|---|---|
| Stories completed | 6/6 (Story 9.0 cleanup carrier + 9.1 Task-0 probes + 9.2 RecordSuccess/Failure/recursion-safe trigger + 9.3 VocabularyDigest + 9.4 AgentLoop digest-injection + 9.5 RecordClickThrough) |
| Reviewer auto-resolved fix-nows | Story 9.0: 0 (clean review) · Story 9.1: 0 (clean review; 2 LOW deferred) · **Story 9.2: 2 MEDIUM + 2 LOW** (ByRef pAuditEmitted envelope-correctness + %OnAfterSave SQLCODE error propagation; 2 LOW deferred) · Story 9.3: 1 MEDIUM-reclassified-LOW + 1 LOW (Build() Catch error capture; removed unused helper; 2 LOW deferred) · Story 9.4: 0 (clean review) · Story 9.5: 0 (clean review) |
| User-led empirical battery findings | **1 user-redirect: live Anthropic NFR-P6 verification was deferred when credentials were locally available** (`.keys` had Anthropic key; `Ens.Config.Credentials.SessionAgentAnthropic` was already configured; `Util.EnvSecret.IsResolvable` returned 1). The lead framed walkthrough options as mutually exclusive and treated comprehensive as "skip live"; user redirected. The live exercise THEN ran clean — Row 250 cross-turn cache hit of 5142 tokens validates NFR-P6 against real wire behavior. |
| Final regression sweep | **366/366 PASS via SQL ground-truth probe (canonical numerical-MAX form)** — Story 9.0 AC-2 reviewer-enforcement codification held across all 5 dev cycles (zero lex-MAX drift; Epic 8 had 3 re-discoveries) |
| Live Anthropic NFR-P6 prompt-cache verification | ✅ Row 250 (turn 2 cross-turn): `RequestTokens=4623, CacheHitTokens=5142` against `claude-haiku-4-5-20251001` — two-array invariant works in production |
| Tool.Registry tool count | ✅ 23 tools (13 inspection + 10 search) — unchanged from Epic 8 close |
| Cycle calendar duration | Started 2026-05-07; closed 2026-05-07 (single-day cycle, 6 commits) |
| Project velocity | 9/10 epics complete (90%) — 9 calendar days from project start |

## What went well (user-confirmed; all 3 wins accepted as the headline summary)

| # | Theme | Evidence |
|---|---|---|
| W-1 | **Rule 6 5th-bullet codification (Story 9.0 AC-1) closed the 4-cycle user-redirect pattern.** | Lead proactively offered walkthrough scope choices BEFORE retro question; user chose "Full per-story walkthrough" without redirect on the structural-minimum compliance. The discipline-rule codification works as a behavioral change mechanism — not just documentation. (Note: a NEW redirect surfaced — see C-1 below — but it's a different shape than the prior 4-cycle pattern; the structural pre-retro checklist worked AS DESIGNED.) |
| W-2 | **Story 9.4 two-array invariant — best architectural decision of the cycle.** | The `tTurns` (canonical, persisted) vs `tTurnsForLlm` (digest-prefixed, provider-bound) split is novel for this codebase. Without it, every replay would re-inject the digest AND persist it. Reviewer's specific OREF-aliasing dual-push concern was addressed cleanly — `tInjectDigest` flag gates the dual-push; pass-through aliasing for inspection-agent path costs zero overhead. Pattern reusable for any future "provider-bound contextual payload that must not persist." |
| W-3 | **Six-story epic completed in a single calendar day with zero lex-MAX drift across 5 dev cycles.** | All 6 commits (`dfb5bd2` → `bb4c804` → `d25076e` → `5a959dc` → `a54f8c3` → `2dd9aea`) landed clean. Reviewer auto-resolved 4 MEDIUM + 4 LOW; 0 HIGH. Mock-provider pattern from Story 5.4 generalized cleanly to AC-4 cached-prefix verification. The disciplined dev/review/commit pipeline produced clean Epic 9 with no out-of-band corrections. **Story 9.0 AC-2 codification (lex-MAX SQL probe form is a MEDIUM finding) eliminated the recurring drift Epic 8 saw 3 times.** |

## What didn't go well (user-selected challenge — C-1)

| # | Theme | Severity | Evidence |
|---|---|---|---|
| **C-1 (user-selected)** | **Live Anthropic NFR-P6 verification was deferred when credentials were locally available.** | MEDIUM (process discipline; same shape as Epic 5 retro AI-1's "user-led empirical battery" gap, different surface) | Lead framed walkthrough options as "Live Anthropic (Recommended)" vs "Full per-story walkthrough" as MUTUALLY EXCLUSIVE. User chose comprehensive; lead treated that as "skip the live probe." User redirect: *"But we have live credentials for Anthropic, it's in `.keys`, why didn't we use those?"* Empirical reality: `Util.EnvSecret.IsResolvable("ANTHROPIC_API_KEY", "SessionAgentAnthropic") = 1`; `Ens.Config.Credentials.SessionAgentAnthropic` was already configured. The lead never probed resolvability — assumed the live test was credential-gated when it wasn't. **The FIRST step of Rule 11 ("live integration smoke test mandatory when external API is in scope") should be to PROBE resolvability, not assume.** Post-redirect, the live exercise ran clean: turn 2 cross-turn CacheHitTokens=5142 against `claude-haiku-4-5-20251001` (Row 250 of `Audit.LlmCall`) — empirically validates NFR-P6 against real wire behavior. The structural mock test alone would have shipped Epic 9 with NFR-P6 unverified. |

## Other findings (lower-priority, codified but not user-selected as headline)

| # | Theme | Severity | Evidence |
|---|---|---|---|
| C-2 | **Story 9.2 reviewer caught 2 MEDIUM defensive-error-propagation gaps the dev shipped without.** | MEDIUM (catch worked) | MEDIUM-9.2-F01 (`ByRef pAuditEmitted` — without ByRef return, vocab_lookup envelope's `audit_emitted` field would silently default to 0); MEDIUM-9.2-F02 (`%OnAfterSave` `SQLCODE < 0` not propagated — silent confidence-write failure). Spec wording "propagate the status per project rule 'Write Status Checking'" was insufficient — didn't enumerate the specific defensive surfaces. Codification opportunity below (AI-3). |
| C-3 | **Story 9.3 token-cap branch (`If tEstTokens > 1200`) is structurally unreachable under current calibration.** | LOW (deferred) | Worst-case digest at MaxEntries=20 × MessageBodyClass MAXLEN=128 is ~833 tokens. Cap fires only if either constant grows past ~30 / ~256. The row-count `MaxEntries` cap is the binding constraint. `TestVocabularyDigestTokenCapEnforced` exercises the row-count path; the byte-count branch has zero coverage. Decision deferred to Story 10.6 (`PurgeStaleSearchChat + UserVocabularyDecay`) which will scale UserVocabulary row counts and may make the cap reachable. |
| C-4 | **Pre-existing test-suite flakes still recur under concurrent cadence (3 stories observed).** | LOW (carry-forward) | Story 9.1 dev observed `AuditTest:LogLlmCallWritesOneRow`; Stories 9.2 + 9.4 observed `AgentLoopGuardsTest:TestRunTurnMaxIterationsCap` + `ToolCallRoundtripIntegrationTest`. All pass on retry. Root cause is global state leaking under concurrent test cadence. Item H (Rule 9 carry-forward in Story 9.0) captures one specific instance; broader pattern is now generalized in AI-5 below. |

## Epic 8 retro action-item follow-through — 5 of 5 honored

First epic in 2 cycles where every prior commitment was discharged.

| Action item | Status | Evidence |
|---|---|---|
| AI-1 (Epic 8) — Codify Rule 6 5th-bullet (per-story walkthrough mandatory pre-retro) | ✅ Completed in Story 9.0 AC-1; **first applied** at THIS retro | Lead emitted scope choices BEFORE retro question; user chose "Full per-story walkthrough" without redirect on the structural-minimum compliance. The 5th bullet works AS DESIGNED — the surface where it failed (C-1 above) is a different gap. |
| AI-2 (Epic 8) — `object-script-testing.md` reviewer-enforcement stanza for lex-MAX form | ✅ Completed in Story 9.0 AC-2; applied throughout Epic 9 | Zero lex-MAX drift across 5 dev cycles (Epic 8 had 3 re-discoveries). Reviewer auto-blocking enforcement makes the codification load-bearing. |
| AI-3 (Epic 8) — Welcome-message rewrite to directive form | ✅ Completed in Story 9.0 AC-3 | `static/chat-panel.js:229–230` reads as directive form; no capability enumeration. Operator-facing surface no longer drifts when new tools ship. |
| AI-4 (Epic 8) — Tool-Description full-qualified-class-name guidance | ✅ Completed in Story 9.0 AC-4 | Both `InspectBodyCandidates` + `SearchByMessageClass` Description parameters carry the appended sentence. LLM-side discoverability gap closed. |
| AI-5 (Epic 8) — Task 0 stale-reference grep at Story 9.0 | ✅ Completed in Story 9.0 Task 0 | No drift discovered beyond AC-3/AC-4 surfaces. Future stories inherit the proactive grep pattern. |
| Story 7.0/8.0 carry-forward E/H/I | ✅ Re-bound to Story 10.9 with explicit drift-history; Item I now on **third** Rule 9 recovery | `deferred-work.md` entries amended; Story 10.9 spec author MUST grep "Story 10.9". |

## Action items (durable; captured for Story 10.0 carry-forward)

5 action items grounded in Epic 9 incidents. Each is triaged for codification in Story 10.0 (Epic 9 deferred cleanup) per the Rule 9 binding-deferral pattern.

| # | Rule / Codification | Source incident | Mechanism |
|---|---|---|---|
| **AI-1** | **Sharpen Rule 11 with a "credential-resolvability probe FIRST" sub-clause.** Codify: *"Before scoping the epic-end empirical battery's walkthrough options, the lead MUST probe `Util.EnvSecret.IsResolvable(envVarName, credentialName)` for every external API in scope. Treat the live integration smoke test as DEFAULT AVAILABLE — only mark it skipped if the probe returns 0. Never assume credentials are absent without empirical verification. Cite Epic 9 retro user-selected challenge C-1 as the originating finding (lead assumed `.keys` content was unconfigured when `Ens.Config.Credentials.SessionAgentAnthropic` was already in place)."* AND extend Rule 6 5th-bullet with a sub-bullet: *"When offering walkthrough scope choices, the lead's first action is the resolvability probe — not the user-question."* | C-1 (user-selected headline). | Append a "Credential-resolvability probe at walkthrough-scoping time" stanza to Rule 11 in `.claude/rules/epic-cycle-discipline.md`. ~10 lines of codification. |
| **AI-2** | **Operator-state checklist (Rule 7) — credential-resolvability matrix at /epic-cycle Step 1.** Codify: *"At /epic-cycle Step 1 (sprint planning), the lead enumerates all configured `Ens.Config.Credentials` rows + `.keys` fallback file content + cross-checks against in-scope external APIs for the epic. Documents the resolvability matrix BEFORE per-story dispatch in `epic-{N}-operator-state.md`. The matrix is the source of truth for which provider live tests can run; any 'skipped' live test must cite a specific resolvability=0 row from the matrix."* | C-1 (sub-cause: lead never built the resolvability matrix at Step 1, so by retro-time the credential availability was an unanswered question). | Append a "Credential-resolvability matrix at Step 1" sub-section to Rule 7 in `.claude/rules/epic-cycle-discipline.md`. ~15 lines of codification. |
| **AI-3** | **Spec-template addition — defensive-surface enumeration in 'propagate the status' AC clauses.** Codify: *"When an Acceptance Criterion specifies 'propagate the status per project rule Write Status Checking', the spec MUST also enumerate the specific defensive surfaces required for the change: (a) ByRef returns when envelope-correctness depends on the inner result (e.g., `ByRef pAuditEmitted` for vocab_lookup save mode); (b) `If SQLCODE < 0` checks on every `&sql` operation that mutates state; (c) error-envelope shaping when downstream callers expect structured error responses. Reviewer enforces: if the spec relies on the project rule alone without enumeration, missing defensive surface is a MEDIUM finding per Rule 8."* | C-2 (Story 9.2 MEDIUM-F01 + F02 — spec wording was rule-reference only). | Append a "Defensive-surface enumeration in 'propagate the status' AC clauses" sub-clause under Rule 8 in `.claude/rules/epic-cycle-discipline.md`. ~10 lines of codification. |
| **AI-4** | **Story 9.3 token-cap branch — defer decision to Story 10.6.** LOW-9.3-F02 already in `deferred-work.md`; bound to "future hardening." Story 10.6 spec author should re-evaluate after `UserVocabularyDecay` sweep affects row count distributions. Either accept as forward-proofing or remove the dead branch. | C-3 (token-cap branch unreachable under current calibration). | No new codification — re-bind LOW-9.3-F02 owner to Story 10.6 spec author. |
| **AI-5** | **Generalize the test-isolation flake pattern in `deferred-work.md`.** Currently Item H captures `MultiNamespaceInstallTest` only; AuditTest + AgentLoopGuardsTest + ToolCallRoundtripIntegrationTest exhibit the same global-state-pollution shape under concurrent cadence. Add a single umbrella entry naming the pattern + the 4 affected test classes. Bind to Story 10.9 alongside Items E/H/I per Story 9.0 AC-6. | C-4 (3 stories observed flakes; broader pattern unowned). | One new `deferred-work.md` entry — "Test-suite global-state pollution under concurrent cadence" — naming the 4 affected classes + binding to Story 10.9. ~15 lines. |

**Capture surfaces** (load-bearing for Story 10.0):
- **Rule files**: `.claude/rules/epic-cycle-discipline.md` Rule 6 5th-bullet sub-bullet (AI-1); Rule 7 §"Credential-resolvability matrix" (AI-2); Rule 8 §"Defensive-surface enumeration" (AI-3); Rule 11 §"Credential-resolvability probe at walkthrough-scoping time" (AI-1).
- **`deferred-work.md`**: AI-5 umbrella entry for test-isolation flakes; LOW-9.3-F02 owner re-bound to Story 10.6 (AI-4).

## Continued deferrals (genuine Rule 8 passes from Epic 9)

- **Story 7.0 carry-forward items E / H / I** (originally bound at Story 8.0 → re-rebound to Story 9.0 → re-rebound to Story 10.9 per Story 9.0 AC-6) — all 3 still bound to Story 10.9 per Rule 9. Item I is now on its **third Rule 9 recovery** (Story 4.7 → Story 8.0 → Story 9.0 → Story 10.9); if Story 10.9 also can't carry it, the recovery note compounds (fourth-recovery → dedicated cleanup story rather than further re-binding).
- **MEDIUM-9.1-F01** — pre-existing `AuditTest:LogLlmCallWritesOneRow` flake (now part of AI-5 umbrella entry).
- **LOW-9.1-F02** — `NormalizeValue` boolean-type doc-comment drift (cosmetic; Rule 8 test-3).
- **LOW-9.2-F06** — `RecordSuccess`/`RecordFailure` empty-input persistence-layer guard (caller-side guard ships in Story 9.5; persistence-layer remains deferred to future hardening pass).
- **LOW-9.2-F08** — `%OnAfterSave` Confidence-recompute race under concurrent saves (out of AC-3 scope; single-operator-per-session usage means race window is operationally inaccessible in v1).
- **LOW-9.3-F01** — `Build` reader of `MessageBodyClass` lacks `$Char(0)` defensive normalization (unreachable today; structurally; deferred to first SQL-UPDATE write path on UserVocabulary string column).
- **LOW-9.3-F02** — Token-cap branch unreachable under current calibration (re-bound to Story 10.6 per AI-4).

## Significant discoveries

**One — Rule 6 5th-bullet codification works AS DESIGNED for the structural minimum.** First retro where the user did NOT redirect with "you forgot to do X" on the pre-retro empirical battery's structural minimum (4 bullets emitted verbatim before the retro proposal). The 5-cycle user-redirect pattern (Epic 1 / Epic 2 / Epic 7 / Epic 8) is broken at the structural level. The new C-1 redirect (live-credential resolvability) is a DIFFERENT gap — at the substantive level, not the structural minimum.

**Two — Live Anthropic NFR-P6 verification empirically passed against real wire.** Row 250 cross-turn CacheHitTokens=5142 (against `claude-haiku-4-5-20251001`) confirms Story 9.4's two-array invariant works in production. The mock test's structural claim ("system + tools byte-identical") holds against real Anthropic prompt-cache. NFR-P6 is now verified-with-evidence, not just verified-with-mock.

**Three — Story 9.0 AC-2 codification (lex-MAX SQL probe form is a MEDIUM finding) eliminated the recurring drift.** Epic 8 had 3 re-discoveries of the lex-MAX trap (Stories 8.2 / 8.4 / 8.6). Epic 9 had ZERO re-discoveries. Reviewer-blocking enforcement is what makes the codification load-bearing — the rule alone (without the enforcement stanza) was insufficient.

**Four — The user-redirect pattern shifted from structural to substantive.** Across Epic 1 / Epic 2 / Epic 7 / Epic 8, the redirect was "you forgot to run the structural empirical battery." Epic 9's redirect was "you ran the structural battery but missed the substantive live verification." This is a positive evolution — the structural baseline is automatic; the substantive gap is the new frontier. AI-1 + AI-2 codify the substantive gap so Epic 10 starts at the new baseline.

## Lessons applied from Epic 8 retro

| Epic 8 commitment | Status in Epic 9 |
|---|---|
| AI-1 — Codify Rule 6 5th-bullet | ✅ Codified in Story 9.0 AC-1; **first applied** at THIS retro — structural minimum compliance achieved without redirect. |
| AI-2 — `object-script-testing.md` reviewer-enforcement stanza | ✅ Codified in Story 9.0 AC-2; applied throughout Epic 9 — ZERO lex-MAX drift across 5 dev cycles. |
| AI-3 — Welcome-message rewrite to directive form | ✅ Codified in Story 9.0 AC-3; verified rendered on disk + matching test class assertion. |
| AI-4 — Tool-Description full-qualified-class-name guidance | ✅ Codified in Story 9.0 AC-4; both tool Description parameters carry the appended sentence. |
| AI-5 — Task 0 stale-reference grep | ✅ Codified in Story 9.0 Task 0; no drift discovered beyond AC-3/AC-4 surfaces. |
| Story 7.0/8.0 carry-forward E/H/I | ✅ Re-bound to Story 10.9 (Item I now on third Rule 9 recovery). |

**No missed lessons** — every Epic 8 commitment was honored in Epic 9. Velocity holding at 6 stories/calendar-day.

## Next epic preparation — Epic 10: Search Agent — UI Embed, Hand-off & TTL Sweep

**Scope:** 9 stories (10.0 cleanup + 10.1–10.9). Story 10.1 ships `EnsPortal.MessageViewer` subclass that hosts the `[ZenMethod]` wrapper delegating to Epic 9's `VocabCapture.RecordClickThrough`. Story 10.6 ships `PurgeStaleSearchChat` + `UserVocabularyDecay` sweep tasks (which may make the AI-4-deferred token-cap branch reachable). Story 10.9 carries the 4 deferred-work items (E/H/I + AI-5 umbrella) for PRD v1 final triage.

**Dependencies on Epic 9:**

- ✅ `SessionAgent.Search.VocabCapture.RecordClickThrough` ClassMethod — substrate for Story 10.1's `[ZenMethod]` wrapper.
- ✅ `SessionAgent.Search.VocabularyDigest.Build` — substrate for Story 10.2's UI rendering of the digest content.
- ✅ `UserVocabulary.RecordSuccess`/`RecordFailure` + recursion-safe `%OnAfterSave` — substrate for Story 10.6's decay sweep (which decrements Confidence on stale rows).
- ✅ AgentLoop digest-injection — verified live against Anthropic; Epic 10 UI builds on top.

**No blocking gaps.** Epic 10 stories can begin immediately.

**Story 10.0 — Epic 9 cleanup carrier (must run at /epic-cycle epic 10 Step 2):**

When `/epic-cycle epic 10` runs, Step 2 (Retrospective Review & Story X.0) MUST read this retrospective doc AND `deferred-work.md`. Inputs for Story 10.0:

- Epic 9 retro AI-1 codification: Rule 11 §"Credential-resolvability probe at walkthrough-scoping time" (~10 lines) + Rule 6 5th-bullet sub-bullet.
- Epic 9 retro AI-2 codification: Rule 7 §"Credential-resolvability matrix at Step 1" (~15 lines).
- Epic 9 retro AI-3 codification: Rule 8 §"Defensive-surface enumeration in 'propagate the status' AC clauses" (~10 lines).
- Epic 9 retro AI-4: re-bind LOW-9.3-F02 owner to Story 10.6 spec author.
- Epic 9 retro AI-5: add umbrella `deferred-work.md` entry for test-isolation flake pattern (~15 lines).
- Story 7.0/8.0/9.0 carry-forward bindings (items E/H/I): ALREADY rebound to Story 10.9 per Story 9.0 AC-6 — Story 10.0 just confirms binding still holds.

Story 10.0 should fit cleanly under the Rule 1 250-line spec cap (~50 lines of codification + carry-forward confirmation).

## Critical-path items before Epic 10 starts

- **No critical-path code items** beyond Story 10.0's carry-forward work.
- **Apply the new Rule 11 §"Credential-resolvability probe"** (per Epic 9 AI-1) at /epic-cycle epic 10 Step 1 — build the credential-resolvability matrix for Epic 10's external APIs.
- **Apply the new Rule 7 §"Credential-resolvability matrix"** (per Epic 9 AI-2) at /epic-cycle epic 10 Step 1 alongside the operator-state checklist.
- **Apply Rule 6 pre-retro 5-bullet checklist + new walkthrough-scoping sub-bullet** (per Epic 9 AI-1) at Epic 10 close.
- **Apply SQL-probe-as-ground-truth reviewer enforcement** (per Epic 8 AI-2 + Story 9.0 AC-2) for every "N/N test pass" claim — continues to hold.
- **Apply operator-observable surface enumeration** (per Epic 7 AI-1) for every Epic 10 story shipping a `%Persistent` class or operator-observable artifact (Story 10.6 sweep tasks particularly).

## Significant discoveries (epic-update potential)

**No epic plan changes required.** Epic 10's plan is sound — Story 10.1 portal subclass + Story 10.2 UI rendering + Story 10.3 click-through wiring + Story 10.4 hand-off stripe + Story 10.5 concurrent-tab banner + Story 10.6 sweep tasks + Story 10.7 vendored Markdown bundle + Story 10.8 token-resolution + Story 10.9 PRD v1 validation walkthrough. The Story 9.5 `VocabCapture.RecordClickThrough` substrate directly supports Story 10.1's `[ZenMethod]` wrapper. No story rewrites needed.

**Epic 9 walkthrough surfaced one minor scope-clarification for Epic 10:** the live-Anthropic NFR-P6 verification is now a known-passing path. Story 10.9's PRD v1 validation walkthrough should re-run the same probe as part of the final close-out battery (CacheHitTokens > 0 on cross-turn cache test).

## Readiness assessment

| Dimension | Status |
|---|---|
| Testing & quality | ✅ 366/366 sweep at Epic 9 close. Live Anthropic NFR-P6 verified empirically (Row 250). 4 reviewer-caught MEDIUM auto-fixed across the cycle. |
| Deployment | ✅ All 6 commits pushed; HEAD = `2dd9aea`. |
| Stakeholder acceptance | ✅ N/A — single-stakeholder hobby project; Project Lead validated all 6 stories + comprehensive walkthrough including live Anthropic NFR-P6 cross-turn cache test. |
| Technical health | ✅ All Epic 9 classes compile clean; auto-sync working; 23 tools registered (unchanged); UserVocabulary recursion-safe trigger live-verified. |
| Unresolved blockers | ✅ Zero. C-1 user-redirect resolved inline (live exercise ran clean). |
| Sample production state | ✅ Step-1 Bootstrap held throughout cycle. |
| Search Agent backend (Epic 9 acceptance gate) | ✅ MET — vocabulary persistence is recursion-safe; digest assembly + first-turn prefix injection preserve Anthropic prompt-cache (live-verified); click-through capture entry point ready for Epic 10 UI. |
| Audit-volume bound (NFR-SC4) | ✅ No new triples added by Epic 9 (re-used VocabWrite/clickthrough + VocabWrite/extracted from Story 1.3). |
| Next-epic dependencies | ✅ Zero blockers. VocabCapture + VocabularyDigest + UserVocabulary triggers all verified ready for Epic 10 UI consumption. |

**Epic 9 is fully complete and Epic 10 is unblocked.** PRD FR22 (vocabulary capture) + FR24 (first-user-message digest prefix) + NFR-P6 (Anthropic prompt-cache preserved) + Epic 9 acceptance gate all empirically met; Search Agent vocabulary-learning backend foundation is complete and operator-trusted for Epic 10's UI integration.

## Commitments made

- 1 NEW Rule 11 §"Credential-resolvability probe" stanza (AI-1) — Story 10.0.
- 1 NEW Rule 6 5th-bullet sub-bullet (AI-1) — Story 10.0.
- 1 NEW Rule 7 §"Credential-resolvability matrix" sub-section (AI-2) — Story 10.0.
- 1 NEW Rule 8 §"Defensive-surface enumeration" sub-clause (AI-3) — Story 10.0.
- 1 NEW `deferred-work.md` umbrella entry — test-isolation flake pattern (AI-5) — Story 10.0.
- 1 OWNER re-bind — LOW-9.3-F02 to Story 10.6 spec author (AI-4) — Story 10.0.
- 4 carry-forward deferred-work items (E / H / I + AI-5 umbrella) — Story 10.9 via Rule 9 binding.
- This retrospective doc available for Epic 10's mandatory retrospective-review gate.
- API keys for OpenAI / Anthropic / Gemini / Ollama instance retained in `Ens.Config.Credentials` for Epic 10+.

## Next steps

1. Mark `epic-9-retrospective: done` in `sprint-status.yaml` (this commit does it).
2. Optional pause. Epic 9 closes the Search Agent vocabulary-learning backend foundation: 4 new persistence/utility classes (`SynthesizeAlias`, `VocabularyDigest`, `VocabCapture`, recursion-safe trigger on `UserVocabulary`) + AgentLoop digest-injection + 21 new test methods. Project status: 9/10 epics complete in 9 calendar days; 1 epic remains (Search Agent UI Embed + TTL Sweep + PRD v1 validation walkthrough).
3. Begin Epic 10 (Search Agent — UI Embed, Hand-off & TTL Sweep) when ready via `/epic-cycle epic 10`. Story 10.0 will pick up the 5 action items above + the 4 carry-forward deferred-work items per Rule 9 binding-deferral practice.

---

## Project Lead reflections (2026-05-07)

_The Project Lead's user-redirect during the walkthrough scoping ("But we have live credentials for Anthropic, it's in `.keys`, why didn't we use those?") IS the headline lesson of this epic. The lead framed walkthrough options as binary (live OR comprehensive), treated the user's "comprehensive" choice as "skip live," and never probed `Util.EnvSecret.IsResolvable`. The redirect surfaced the substantive gap; the live exercise then ran clean (Row 250 cross-turn CacheHitTokens=5142 against claude-haiku-4-5). The codifications AI-1 (credential-resolvability probe) + AI-2 (credential-resolvability matrix at Step 1) close this gap structurally so Epic 10's lead starts with the resolvability question already answered. The Project Lead also accepted the 3 wins (W-1 Rule 6 5th-bullet codification effective; W-2 two-array invariant architectural; W-3 zero lex-MAX drift across 5 dev cycles) and the 4 challenges as-captured without addition._
