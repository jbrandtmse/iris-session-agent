I'm wanting to create a IRIS Session Inspection Agent similar to the one specied in sources/iris-session-chat, except I don't wat to have any dependecies in the unreleased AI Hub functionality.   Featurewise we should include the features specified in that README, product brief, prd and architecture documents for that project.  
We should also leverage the research that was done to impliment it.  Again the exception being we WILL NOT be using the AI hub functionality so we will need to build libraries and abstractions ti cover the functionality it would have provided.  We also need to create zen pages for configuration of the agents.   Well will also add a similar agent to the message search page to assist in finding sessions.   
In addition to the session chat agent, we should build an agent on the message search screen to assist with searching for sessions.
I want each of the two agents to be configurable seperately.  You should be able to choose models from OpenAI, Anthropic, Google (Gemini), and Ollama.  So the provider framework should be extensible.  (We can start with OpenAI)   API keys should be stored internally, perhaps with the ensemble credential store.
Chat Sessions for Interoperability Sessions should store history per user/per session.  We should probably figure out how to tie those chat to the Interoperability session so that it can be purged when the interoperability session gets purged.  This might mean adding the chat messages to the end of the session with their own Ens.MessageHeader, or some other technique.
The message search agent should have some provision for learning how to search the terms that the user is using, so that it gets easier to find messages.   The message search agent also needs to be smart about the fact there may be millions of messages to search through so indexes, constrained searches and incremental searches are important.
This solution must be installable with IPM.